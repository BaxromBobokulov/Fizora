// Display-only reward values shown in gamification copy (e.g. the Daily
// Challenge card). Actual XP awarded on completion is calculated in
// store/useProgressStore.ts — keep this in sync if that value changes.
export const DAILY_CHALLENGE_XP_REWARD = 25;