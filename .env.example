# Copy this file to .env and fill in real values.
# .env is gitignored and never reaches EAS Build servers directly —
# EXPO_PUBLIC_* values used in builds must also be set via `eas env:set`
# (see README.md for setup instructions).

EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
