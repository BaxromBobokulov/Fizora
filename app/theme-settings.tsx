import { PlaceholderScreen } from "@/components/navigation/PlaceholderScreen";

export default function ThemeSettings() {
  return (
    <PlaceholderScreen
      showBackButton
      icon="color-palette-outline"
      title="Theme"
      subtitle="Light and dark theme switching is coming soon."
    />
  );
}
